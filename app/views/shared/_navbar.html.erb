<nav class="navbar rs-navbar navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% brand_path = root_path %>
      <% brand_path = account_profile_path if signed_in? %>

      <%= link_to(brand_path, class: "navbar-brand") do %>
        <i class="fa fa-paper-plane" aria-hidden="true"></i> Remets
      <% end %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <% if signed_in? %>
      <ul class="nav navbar-nav navbar-left">
        <li class="<%= active_link?("evaluations") %>">
          <%= link_to "Evaluations", evaluations_path %>
        </li>

        <li class="<%= active_link?("submissions") %>">
          <%= link_to "Submissions", submissions_path %>
        </li>

        <li class="<%= active_link?("profiles") %>">
          <%= link_to "Upcoming evaluations", account_profile_path %>
        </li>
      </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li class="dropdown">
            <%= link_to "#", data: { toggle: "dropdown" }, html: { role: "button", aria_haspopup: "true", aria_expanded: "false" } do %>
              <%= current_user.name %><span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu">
              <li><%= link_to "Settings", edit_account_profile_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Sign out", auth_destroy_path %></li>
            </ul>
          </li>
        <% else %>
          <li class="<%= active_link?("authentications") %>">
            <%= link_to "Sign in", auth_new_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
