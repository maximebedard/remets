<%= form_for @submission, html: { multipart: true } do |f| %>
  <%= f.label :documents_attributes %>
  <%= f.file_field :documents_attributes,
    multiple: true,
    name: 'submission[documents_attributes][][file]' %>
  <%= f.submit %>
<% end %>
